/* analytics_stub.js
   este arquivo so eh carregado se o usuario aceitar "analytics".
   troca o conteudo por GA4, Plausible, etc.
*/
(() => {
  // exemplo minimo: marca um evento de pageview no console.
  // remove isso quando plugar uma ferramenta real.
  console.log("[ka] analytics enabled (stub) - pageview:", location.pathname);
})();
